# 개많이잡아 — 남은 작업

> **플랫폼: PC (가로 1280x720 기준).**
> 한때 세로 모바일로 오해해 하네스와 로비를 9:16 으로 맞췄다가 되돌렸다.
> 전제가 틀리면 도구를 고쳐도 방향만 반대로 틀린다.

한 번에 하나씩 끊어서 진행한다. **블록 하나 = 한 번의 착수와 한 번의 완료 보고.**
각 블록의 「완료 기준」을 만족해야 다음으로 넘어간다. 미측정은 통과가 아니다.

진행 표기: `A-1 작업 시작` → (작업) → `A-1 완료` → `A-2 작업 시작`

---

## A. 검증 — 다른 것보다 먼저

### ~~A-1. 최종 통합 실측~~ — 완료
`[클리어] 보스 격파 (등장 1 · 처치 1)` · 820초 완주 · 라운드 12까지 표 확보.
시각 변경 이후 빌드가 끝까지 도는 것을 확인했다.

- [사고] 로비 스크린샷 플래그 초기화를 **매 틱 다시 도는 스폰 루틴 안**에 넣어서
  「초기화 → 촬영 → return」이 무한 반복돼 플레이가 영영 시작되지 않았다. 런당 1회 도는 곳으로 옮겨 해결
- 이 과정에서 후반 난이도의 **진폭**이 드러났다 → B-0 으로 분리

### ~~A-2. 회귀 방지 — 아군 오사 자동 검사~~ — 완료
`Damageable.TakeDamage` 입구에서 같은 팀 출처를 센다. 무적 판정보다 **앞**이라 막힌 시도도 잡히고,
스킬뿐 아니라 자동공격·지속피해 등 **모든 경로**를 덮는다.

- 검증: 합성 자해 1회를 넣으니 `[자해] 1회 · 최대 1.0 · OBJ_Player(Clone) -> OBJ_Player(Clone) (Friend)` 로 잡혔다.
  제거 후 `[자해] 없음` 복귀
- 부수 소득: 길목 가드를 꺼도 오사가 안 났다 — **호출처의 시드 검증과 길목 가드가 각각 독립적으로** 막는다

---

## B. 코어 루프의 구멍 — 게임이 성립하는 데 필요

### ~~B-4. 플레이어가 두 마리 스폰되고 있었다~~ — 완료 (그리고 밸런스를 무효화했다)
`StartScreen` 의 비동기 흐름이 하나를 세우고, 하네스가 또 하나를 세웠다.
**스킬이 두 벌 나가** 그동안의 모든 밸런스 실측이 두 배 화력 위에서 잡혔다.

- 증거: 같은 조건 70초에 **킬 44 → 17**
- 세로 화면비로 바꾸기 전까지 둘째는 화면 밖이라 **보이지도 않았다**
- 스폰 순서를 맞추는 대신 양쪽 모두 「있으면 그걸 쓴다」로 막았다 —
  순서에 기대는 규칙은 경로가 늘 때마다 다시 깨진다

### ~~B-5. 밸런스 전면 재측정~~ — 완료
정상 화력 기준으로 다시 잡았다. 최종: 라운드 피해 2.45~4.0 · 회복 천장 0.0145 · 보스 3,888.

| 런 | 라운드 7~12 HP | 결과 |
|---|---|---|
| 1 | 95 → 82 → 71 → 44 → **27** | 라운드 11 사망 (615초) |
| 2 | 100 → 96 → 100 → 80 → **70** → 81 | 클리어 |
| 3 | 100 → 91 → 100 → 100 → 99 → **88** | 클리어 |

- 클리어 2/3, 사망은 라운드 11 — 초반에 끊기지 않고 끝까지 가서 무너진다
- 100% 를 유지하는 판이 없다. 모든 판이 실제로 깎인다
- 보스 체력은 실측 DPS(약 49.5)에서 역산했다. 8,640 이면 175초가 걸려 12라운드 안에 못 잡는다
- [사고] 화력이 절반으로 바뀐 **직후에** 피해 +15% 와 회복 삭감을 동시에 얹어 라운드 7 에서 죽었다.
  큰 변수가 하나 바뀐 뒤에는 한 번에 하나만 만진다. 양끝을 잡고 이분법으로 좁히는 편이 빠르다
- 남은 편차(27 vs 88)는 파츠 뽑기에서 온다. 더 좁히려면 수치가 아니라 드랍을 손대야 한다


### ~~B-0. 후반 난이도 진폭~~ — 완료 (기준은 수정했다)
같은 데이터로 3회 측정했는데 결과가 갈렸다.

| 런 | 라운드 9~12 HP | 결과 |
|---|---|---|
| 가파른 표 ① | 64% / 65% | 클리어 |
| 가파른 표 ② | 52% → 31% | 라운드 10 사망 |
| 완만한 표 ③ | 87~100% | 클리어(무풍) |

원인은 라운드 표가 아니다. **흡혈 상한이 최대 체력 비례**라, 흡혈을 뽑은 판은 HP 가 100% 에
고정되고 못 뽑은 판은 무너진다. 0 아니면 100인 스탯이라 라운드 표로는 진폭이 안 잡힌다.

**조치**: 기본 처치 회복 1.0 을 깔고(굶는 판 제거), 옵션당 값 2.0 → 1.4 · 초당 상한 0.022 → 0.016
(면역인 판 제거). 시너지 흡혈도 1.2 로 통일.

**결과 (연속 3회)**

| 런 | 라운드 8~12 최저 HP | 결과 |
|---|---|---|
| 1 | 21% | 클리어 |
| 2 | 76% | 클리어 |
| 3 | 67% | 라운드 10 사망 |

| | 이전 | 지금 |
|---|---|---|
| 무풍(100% 유지) 판 | 있었음 | **없음** |
| 각 판 최저 HP | 100 / 65 / 사망 | 21 / 76 / 67 |

- **완료 기준을 잘못 썼다.** 「3회 모두 라운드 12 에서 30~70%」는 **클리어율 100%** 를 요구하는 것이라,
  죽는 게 정상인 로그라이크와 앞뒤가 안 맞는다. 재현성의 지표로는 「라운드 12 시점의 HP」도 나쁘다 —
  그 시점엔 잡졸 스폰이 거의 멈춰 회복이 되므로 판마다 튄다. **구간 최저 HP** 가 옳은 지표다
- 구조적 목표(0 아니면 100 → 기울기)는 달성했다. 남은 편차는 파츠 뽑기에서 오는 것으로,
  더 좁히려면 수치가 아니라 **초반 드랍에 생존 옵션 하나를 보장**하는 식으로 뽑기 자체를 손봐야 한다 → 필요하면 별도 블록

### ~~B-1. 캐릭터 선택~~ — 완료
로비와 스폰 사이를 잇는 `RunSelection` 을 두고, 「영웅」 탭을 실제 선택 화면으로 만들었다.

| 슈퍼독 | 고유 스킬 | 계열 |
|---|---|---|
| 시바 | 개무시 | 각성 |
| 코기 | 식빵 썰기 | 근접 |
| 그레이하운드 | 잔상 질주 | 장판 |

- 실측으로 3종 전부 확인 (`슈퍼독=코기(#2) 고유스킬=식빵 썰기 · 선호계열=Melee` 등)
- 하네스도 `god:60:2` 처럼 캐릭터를 지정할 수 있다 — 안 그러면 바뀌는지 잴 방법이 없다
- [사고] 하네스가 요청 스펙을 `BeginRun()` **뒤에** 읽어, 코기를 지정해도 시바 고유 스킬이 들어갔다.
  고른 캐릭터는 고유 파츠를 끼우기 **전에** 정해져야 한다
- [사고] 영웅 카드를 Build 때 한 번만 그려 세 칸 모두 「고유 스킬 없음」이었다.
  파츠 테이블은 비동기로 늦게 로드된다 — 탭 진입 시와 로드 완료 시 다시 그린다

### ~~B-2. 보스를 진짜 보스로~~ — 완료
보스 전용 적 타입 `5006 납치선` 을 만들고 웨이브가 id 로 직접 부른다.

- `weight: 0` 이라 가중치 추첨에서 완전히 빠진다 — 잡졸로는 절대 안 나온다
- 체력 8,640 (240 × 3.6 라운드 × 10) — 기존 보정값 유지. 속도 4.16 vs 플레이어 5 (쫓아오되 따돌릴 수 있다)
- 단일 프레임이던 `boss_ufo` 를 적 스프라이트 규칙(`alien_ufo_walk0..3`)에 맞춰 **호버 4프레임**으로 냈다.
  걷지 않는 개체라 걷기 대신 떠 있는 움직임을 넣는다 — 가만히 있으면 배경 오브젝트로 읽힌다
- 실측: 등장 1 · 처치 1 · 클리어. 인게임 스크린샷에서 잡졸 무리 속에서도 구분된다
- [사고] 정예 틴트를 보스에도 씌워 납치선이 **주황 덩어리**로 보였다.
  틴트는 「똑같이 생긴 잡졸 중 정예」를 가리키는 장치라, 제 스프라이트가 있는 개체에는 해로울 뿐이다

### ~~B-3. 레벨업 체감~~ — 완료
레벨업이 조용하다. 성장 축을 파츠로 몰았어도 레벨은 오르므로, 오른 것 자체는 보여야 한다.

- 완료 기준: 레벨업 순간 화면에 표시가 나오고, 스크린샷으로 확인된다

---

## H. PC 화면 구성 (신규)

### ~~H-1. 로비를 PC 가로 레이아웃으로~~ — 완료
지금은 세로 4탭(상점·영웅·메인·스킬) 모바일 구조다. 1280×720 가로에 맞는 구성으로 다시 짠다.

- 하단 가로 탭바 → **좌측 세로 내비**, 메인은 좌(주인공)/우(제목·시작) 2단
- [사고] 기준 해상도를 1080×1920 → 1280×720 으로 바꾸자 **모든 글자가 상대적으로 2.7배**가 됐다.
  화면비만 바꾸고 크기를 안 바꾸면 레이아웃이 통째로 어긋난다

### ~~H-2. 캐릭터 도감~~ — 완료
메인 화면에서 슈퍼독을 모아 보는 화면. 잠긴 것도 보여야 한다(기대감).

- 슈퍼독 3종을 가로로. 하단에 「슈퍼독 3종 · 스킬 27종」 집계
- 지금은 3종이 전부 해금이라 보유/미보유가 안 갈린다. E-2 가 들어오면 잠긴 칸을 실루엣으로

### ~~H-3. 스킬 트리~~ — 완료
스킬 27종을 계열별로 펼쳐 보여주고, 해금 상태를 표시한다. E-2(슈퍼 DNA 해금)와 이어진다.

- 완료 기준: 계열별로 묶여 보이고, 해금 여부가 표시된다

### ~~H-4. 설정 — 해상도~~ — 완료
기본 **1280×720**, 그 밖에 일반적인 16:9 프리셋(1600×900 · 1920×1080)과 전체화면 토글.

- `DisplaySettings` — 1280×720(기본) · 1600×900 · 1920×1080 + 전체화면. `PlayerPrefs` 저장
- 16:9 프리셋만 준다. 임의 입력을 받으면 이상한 화면비가 들어와 UI 가 깨진다
- [주의] 에디터에서는 `Screen.SetResolution` 이 Game 뷰를 바꾸지 않는다.
  하네스가 Game 뷰 크기를 따로 잡는 이유가 이것 — 둘을 헷갈리면 「설정이 안 먹는다」로 오진한다

## C. 조작 — PC

### ~~C-1. PC 조작 정리~~ — 완료
PC 이므로 가상 조이스틱은 필요 없다. `VirtualJoystick.cs` 는 쓰이지 않으면 지운다.

- 키보드 이동 + (필요하면) 마우스 조준이 실제로 도는지 확인
- 완료 기준: 키보드로 8방향 이동이 되고, 조작 관련 미사용 코드가 남지 않는다

### ~~C-2. 실측 화면비 고정~~ — 완료 (PC 로 정정)
`GameViewSizer` 가 플레이 진입 전에 Game 뷰를 **1280×720(16:9) 고정 해상도**로 맞춘다.
처음엔 세로 모바일로 오해해 9:16 으로 맞췄다가 되돌렸다.
Game 뷰 크기는 공개 API 가 없어 내부 타입을 리플렉션으로 다루므로, **실패해도 측정은 계속되게** 했다.

- 바꾸자마자 값을 했다 → **D-3** 을 발견. 가로에서는 안 보이던 문제다
- [교훈] 룩 판정의 전제는 타깃 화면비다. 틀린 화면비로 UI 를 고치면 고칠 때마다 다른 곳이 깨진다
  (로비 레이아웃을 세 번 고쳤는데 매번 다시 겹쳤던 이유가 이것이었다)

---

## D. 연출 — 이름과 그림 맞추기 (2차)

### ~~D-1. 남은 범용 이펙트~~ — 완료
3계열이 아니라 **7종**이었다. 「보호」 하나를 4개 스킬이 돌려썼는데 실제로는
보호막·각성·질주·도발로 전부 다른 것이었고, 「눈빛」도 지속 빔과 관통 시선으로 갈린다.

`fx_shield` · `fx_awaken` · `fx_zoom` · `fx_taunt` · `fx_eyebeam` · `fx_gaze` · `fx_static`

- 그림 생성 완료(24종 106프레임), 프리팹 주소 등록 완료, 스킬 5개 재배정 완료
- **남은 것**: 배치 빌드로 프리팹을 굽고 인게임 스크린샷으로 확인
- 완료 기준: 스크린샷에서 이름과 그림이 맞는다

### ~~D-3. 이펙트가 화면을 뒤덮는다~~ — 완료
세로 화면의 카메라 가로 시야는 약 10유닛인데, 으르렁 반경 3(지름 6유닛)이 **화면 폭의 60%** 를 차지한다.
게다가 여러 벌이 겹쳐 벽처럼 보인다 — 3초 쿨인데 5겹이 남아 있다.

- 먼저 볼 것: 연출이 제때 풀로 반환되는가. 겹침이 원인이면 크기는 건드릴 필요가 없다
- 그 다음: 광역 연출은 꽉 찬 그림 대신 **테두리 링** 이 낫다. 같은 반경이어도 시야를 안 막는다
- 원인은 크기가 아니라 **누수**였다. 즉발 스킬은 `DespawnAfterHits: 1` 인데 **맞춘 게 있어야** 펄스가 돈다.
  사거리에 적이 없으면 영영 안 사라져 빈 벌판에서 계속 쌓였다
- 「못 맞혔으면 0.6초 후 회수」 + 「무슨 일이 있어도 12초」 두 겹으로 막았다

### ~~D-2. 적 방향 스프라이트~~ — 완료
플레이어만 4방향이고 적은 단방향이다. 물량이 많아 눈에 띈다.

- 완료 기준: 적이 진행 방향을 본다

---

## E. 메타 — 런 밖 성장 (기획서 미구현분)

### ~~E-1. 진행도 저장~~ — 완료
`PlayerPrefs` 조차 안 쓴다. 저장이 0 이라 껐다 켜면 전부 초기화된다.

- 완료 기준: 클리어 기록·해금 상태가 재실행 후에도 남는다

### ~~E-2. 슈퍼 DNA 해금~~ — 완료
기획서의 「스킬을 아이템으로 해금」이 미구현. 스킬 27종이 처음부터 전부 드랍 풀에 있다.

- 완료 기준: 잠긴 스킬은 드랍되지 않고, 해금하면 풀에 들어온다

### ~~E-3. 상점~~ — 완료
둘 다 `기획 대기` placeholder. **E-1, E-2 가 끝나야 채울 내용이 생긴다.**

- 완료 기준: 두 탭이 실제 데이터를 보여준다

---

## X. 잔여 결함 (작음)

### ~~X-1. 결과 화면 딤~~ — 완료
`CLEAR!`/`GAME OVER` 뒤로 게임 화면이 그대로 비친다. `Image` 에 흰 스프라이트를 넣어 고쳤다고
생각했는데 스크린샷에선 여전히 밝다. 픽셀로 재봤지만 **미딤 상태의 같은 장면**이 없어 확정하지 못했다.

**진짜 원인은 딤이 아니었다.** 종료 스크린샷을 `EndGame()` **이전에** 찍고 있어서,
지금까지 **결과 화면을 한 번도 제대로 본 적이 없었다.** 밝은 배경에 텍스트만 뜬 그림들은
결과 화면이 아니라 게임 화면에 글자가 겹친 것이었다.

- 진단을 리포트에 찍어 확인: `enabled=True sprite=있음 alpha=0.80 rect=1280x720` — 딤은 정상이었다
- 촬영을 런 종료 뒤로 옮기고, 알파 0.8 → 0.92, 라운드 배너는 결과 표시 시 내린다
- 실측: 딤 위 잔디 (68,84,35) → (32,49,22)
- 함께 고침: 런타임 생성 텍스처에 `HideAndDontSave` — 씬 리로드에서 정리되면
  스프라이트는 살아 있는데 텍스처가 없어 아무것도 안 그려진다

## ~~F. 사운드~~ — 완료
`build_sfx.py` 로 WAV 8종을 **절차 생성**했다. 사운드 에셋이 하나도 없어 아트와 같은 방식으로 만들었다.
`SfxManager` 가 소스 12개를 돌려 쓰고, 같은 소리가 겹쳐 찢어지지 않게 최소 간격을 둔다.

- 실측: `효과음 클립=8종 재생=91회` (60초 런)
- **한계**: 소리가 *어떻게 들리는지*는 검증 못 했다. 배선과 재생 횟수만 확인했고, 자리 채움 음원이다.
  진짜 음원이 생기면 같은 파일명으로 갈아 끼우면 된다

---

## ~~G. 문서~~ — 완료
- 제목은 이미 **「뱀서류 게임 기획」** 이었다 (내 목록이 틀렸다. 3D 표기는 진작 없어졌다)
- 기획서가 지금 게임과 어긋난 곳을 고쳤다
  - 게임명 `개판치자!` → **개많이잡아** (로비에 이미 이 이름이 나가 있다)
  - 「부위 6칸 제약」 → **슬롯 6칸, 부위 제약 없음** + 왜 버렸는지(계열이 부위에 몰려 조합이 도달 불가능했다)
  - 캐릭터 절: 「고유 파츠가 부위를 잠근다」 → **고유 파츠의 계열이 선호 계열이 된다**
- 「조합(넓게·연계·깊게)」과 「생존 축」 절을 증강 영역에 추가했다
- 라운드 곡선·수치는 넣지 않았다 — 「기획서에는 코어 재미와 스킬·증강만」 지시에 따름

**확인 필요**: 게임명을 `개많이잡아` 로 통일했다. `개판치자!` 로 되돌릴 거면 로비 타이틀도 함께 바꾼다

---

## H · 로비 재편 (완료)

`StartScreen` 이라는 이름이 하는 일과 안 맞았고, 프리팹 안에는 예전 스킬 장착 슬롯(Slot_Q/W/E/R)이
그대로 남아 있었다. 「런타임에 숨긴 것」과 「없앤 것」은 다르다 — 프리팹을 실제로 비웠다.

- `StartScreen` → `LobbyWindow` (게임을 켜면 처음 보이는 창), `LobbyScreen` → `LobbyView` (그리는 쪽)
  - .cs / .prefab / .meta 를 GUID 유지한 채 개명 — 씬과 프리팹 참조가 끊기지 않는다
- 프리팹 오브젝트 7개 → 1개. Canvas·슬롯 전부 제거. 화면은 `LobbyView` 가 코드로 만든다
- 탭 6개(상점·영웅·메인·스킬·도감·설정) → **4개**
  | 탭 | 담은 것 |
  |---|---|
  | 시작 | 제목 · 주인공 · 시작 스킬 · 기록 · 게임 시작 |
  | 캐릭터 | 슈퍼독 3종 고르기 + 사료 영구 강화 3종 (옛 영웅+도감+상점) |
  | 스킬 트리 | 계열 6열 · 슈퍼 DNA 해금 |
  | 설정 | 해상도 3종 · 전체화면 · **데이터 초기화** |
  - 영웅(고르기)과 도감(모아보기)이 같은 화면이었다. 같은 것을 두 군데서 보여주면 둘 다 흐려진다
  - 상점은 「무엇을 키우는가」라 캐릭터 옆에 있어야 한다
- 데이터 초기화: `SaveData.Wipe()` 연결. **두 번 눌러야** 지워진다(1차 → "정말 지운다"),
  탭을 떠났다 오면 안전 상태로 돌아간다
- [사고] 카드 3장이 화면 바닥까지 내려와 아래 붙인 강화 줄을 덮었다. 밴드를 0.25 위에서 끊었다

**측정**: 4탭 전부 1280×720 로 촬영해 겹침·잘림 없음 확인

---

## I · 스킬 모듈화 (ActionScriptKit 참고, A 범위 완료)

`Desktop/ActionScript/ActionScriptKit` 의 「트리거-비헤이버 + 폴더 모듈」 구조를 참고했다.
트리거/비헤이버 분리와 데이터 조립은 **이미 같은 사상**이었으므로, 실제로 빠져 있던 둘만 가져왔다.

### 1. 노드 어휘 자동등록

노드 하나를 붙이려면 세 군데(`_enums.js` · `BTBuilder` switch · 노드 클래스)를 고쳐야 했고,
switch 를 빠뜨려도 **컴파일은 통과하고 런타임에 조용히 아무 일도 안 했다.**

- `[BTNode(ESkillNodeType.…)]` 어트리뷰트 — 클래스가 자기 타입을 스스로 신고
- `BTNodeRegistry` — 리플렉션으로 수집. `BTBuilder` 의 switch 60줄 제거
- **Jinhyeong/GameData/스킬 노드 어휘 검사** — 구현 현황을 찍고 `config.json` 의
  `UnimplementedNodes` 를 실제 상태로 갱신한다 (전에는 사람이 손으로 적어 유지)

[사고] 처음엔 「클래스가 받는가」만 보고 「미구현 없음」이라 판정해, 손으로 적혀 있던
안전장치 3종을 지울 뻔했다. 셋 다 클래스는 받지만 실제로는 죽어 있었다 —
`DespawnAfterBounces` · `DespawnOnWall` 은 `ctx` 에 쓰이기만 하고 `SkillHit` 이 안 읽고,
`TriggerOnOreBreak` 은 `TriggerNode` 의 `default:` 로 떨어져 발동 자체가 안 된다.
**받는 것 ≠ 동작하는 것.** 어트리뷰트에 `Inert = new[]{ … }` 를 따로 두어 구분한다.

### 2. 한 폴더 = 한 스킬

```
전  skills/2002_bark_cone.js  +  modules/stat/bark_cone.js  +  modules/visual/bark.js
후  skills/2002_bark_cone/{ skill.js, stat.js, visual.js }
```

- stat 27종은 **공유 0** 이라 전부 스킬 폴더로. visual 은 23종 중 공유 3종(dust·tail·kibble)만 `modules/` 에 남김
- 폴더에 파일이 있으면 `skill.js` 에 이름을 안 적어도 된다 — 폴더가 곧 소속 (47개 참조 줄 삭제)
- 이름을 적으면 공용을 가리킨 것으로 보고 **적어 준 쪽이 이긴다**
- launch 9종은 7종이 공유라 그대로 `modules/launch/`

**측정**: 옮기기 전 JSON 을 기준선으로 떠 두고 대조 — 트리·수치 전부 동일(경로 문자열만 변경).
DB 검증 27스킬 노드 수 동일. 180초 실플레이 킬 110 · 스킬 6개 발동 · 자해 없음.

### 2-2. 데이터 폴더를 킷 규약에 완전 정렬 (레퍼런스 갱신본 반영)

레퍼런스가 갱신되어 다시 대조했다. 새로 생긴 것은 `ActionScriptResources.cs`(`_` 접두 헬퍼를
먼저 읽는 규약 — 우리 `_enums.js` 와 같은 사상), 샘플 `FireTotem`·`Guard`, 「리더가 하는 일」 절.
사상은 그대로였고, 우리 쪽 정렬이 절반만 되어 있었다.

**파츠·옵션도 스킬 폴더로.** 세어 보니 파츠 27 ↔ 스킬 27 **1:1**, 옵션 159가 전부 파츠 하나에 귀속.
예외가 하나도 없는데 **186개 파일이 자기 스킬에서 떨어져** 있었다(지난번에 옮긴 47개보다 크다).

**폴더 이름의 뜻을 맞췄다.** 예전 `modules/` 는 종류 버킷이라 킷의 `Modules/`(모듈 하나)와
뜻이 **정반대**였다.

```
GameData/
  Modules/2002_bark_cone/   skill.js stat.js visual.js part.js options/{6개}
  Shared/                   launch · visual(공용 3) · buff · debuff · stat(빈)
  Tables/                   enemy · wave · round · synergy
```

- `config.json` 에 `folderOnly: true` 추가 — 파츠·옵션은 스킬 폴더에서만 온다.
  제네릭 스캔을 돌리면 같은 폴더의 `skill.js` 까지 파츠로 집는다
- id 표 검사 순서 교정 — 폴더 것을 합친 **뒤에** 돌게. 안 그러면 파츠 27개가 검사를 건너뛴다.
  **검사를 건너뛴 데이터는 검사가 없는 것보다 나쁘다** (통과했다고 믿게 되므로)

**측정**: 13개 표 전부 행수·내용 동일(`path` 문자열만 변경 — 런타임은 `Skill.json` 과 id 표만
읽고 id 로 조인한다. `SkillStat/Visual/Launch.json` 은 C# 에서 여는 곳이 없다).
실플레이 파츠 27 · 옵션 159 · 스킬 27 정상 로드.

[사고] `skills` → `Modules` 로 바로 이름을 바꿨다가 **윈도우가 `modules` 와 같은 폴더로 보고**
그 안으로 집어넣었다. 이어서 이스케이프가 깨진 PowerShell 한 줄이 그 통째를 `Shared/` 로 옮겼다.
352개 전부 무사했지만, 대소문자만 다른 이름으로는 절대 바꾸지 않는다 —
`modules/` 를 먼저 해체해 이름을 비운 뒤에 썼다.
(PowerShell 5.1 은 BOM 없는 UTF-8 .ps1 을 ANSI 로 읽어 한글 주석이 깨지고 파싱이 터진다.
경로를 옮기는 스크립트는 bash 로 쓴다.)

### 안 가져온 것

- **액터 상태기계** (`[action]`+`[anim]`+`[at]` 타임라인+입력 캔슬 윈도우) — 원본이 XML 액션 게임이라
  「모션 중 특정 프레임 판정」이 뼈대인데, 스킬이 자동 발동하는 뱀서류엔 쓸 자리가 없다
- **텍스트 DSL(.asc)** — 저작자가 한 명이고 `compose.js` 가 이미 20종을 검증한다.
  기획자가 직접 쓰게 될 때 다시 꺼낸다
- **에셋 동거** — 킷은 프리팹까지 모듈 폴더에 넣지만, 우리는 데이터가 `_DataExporter/`(Unity 밖),
  아트가 `Assets/_Art/FX/` 라 데이터만 모였다. 폴더 복붙 이식은 데이터 한정

---

## J · 이펙트를 「간판」 스타일로 (레퍼런스 반영, 완료)

`_Ref/` 에 받은 레퍼 9장의 방향: **외곽만 따서 실루엣으로 눈에 띄게 승부한다.**
발바닥이면 발바닥, 뼈면 뼈. 스킬 이름이 그림 한 장으로 읽혀야 한다.

### 왜 기존 것이 안 읽혔나 (그대로 파라미터가 됐다)

| | 예전 | 지금 |
|---|---|---|
| 크기 | 64px 안에서 작게 | 캔버스를 꽉 채움 |
| 속 | 흰색으로 꽉 참 | 비움 — 선만 남아 아래가 비친다 |
| 외곽 | 얇은 회색 | 아주 두꺼운 어두운 선 |
| 글로우 | 없음 | 좁게 번짐 |

`_DataExporter/tools/build_signfx.py` (신규) — 실루엣 한 장을 넣으면 세 겹으로 굽는다:
글로우 → 어두운 외곽 → 밝은 안쪽선. **레퍼 절반이 검은 배경 전제의 네온**이라
그대로 올리면 밝은 초원에 묻힌다. 어두운 외곽이 밝은 바닥에서, 밝은 안쪽선이 어두운 바닥에서
형태를 지킨다 — 어느 바닥에서도 읽히는 유일한 조합이다.

**레퍼는 두 갈래였다.** 모양이 처리를 정한다:
- 네온형(`hollow=True`) — 큰 단일 실루엣: 발바닥 · 뼈 · 원반 · 눈 · 식빵
- 만화형(`hollow=False`) — 작은 조각 여럿: 왈왈 · 으르렁 · 정전기
  잉크가 양쪽에서 먹으므로 폭이 잉크의 4배가 안 되면 속이 안 남고 검은 덩어리가 된다.

### Orient 데이터화 (핵심 수정)

`LaunchExecutor` 가 「자기버프면 세우고 나머지는 눕힌다」로 **추측**하고 있었다.
그래서 정면에서 본 그림(발바닥 · 뼈 · 이빨 · 원반)이 전부 눕혀져 원근으로 찌그러졌고,
화면에서 무엇이 나갔는지 읽히지 않았다. 스킬의 종류와 그림의 성질은 다른 축이다.

`ESkillParamKey.VisualOrient` 추가 → visual 모듈 23종이 `Orient` 를 들고 →
launch 모듈 8종이 Launch 노드로 전달 → 클라는 읽기만 한다.
Billboard 12 / Ground 11.

### 그린 것 24종

발바닥 · 개뼈 · 왈왈 · 이빨 · 원반 · 테니스공 · 슬리퍼 · 식빵 · 사료 · 제3의 눈 ·
질주선 · 개무시 · 으르렁 · 하품 · 무리울음 · 소나 · 냄새 · 눈빛레이저 · 정전기 ·
발톱 · 꼬리 · 보호막 · 각성 · 폭발

팩 아트로 때웠던 9자리도 같은 문법으로 다시 그려 재지정했다 —
한 화면에 두 문법이 섞이면 둘 다 싸구려로 보인다.

### 사고 기록

- 글로우 5px → 64px 캔버스를 덮어 **흰 사각 얼룩**. 2px 로 낮췄다
- 조각을 눈대중으로 배치 → 세 번 겹쳤다. **호 길이 계산으로 탐색**해 여유 6.6px 확보
- 속 비우기가 내부 디테일을 먹어 공 · 원반 · 눈이 그냥 도형이 됐다 →
  **구멍**을 뚫으면 침식이 구멍 둘레에도 선을 만든다. 공짜로 얻는 디테일
- 그 구멍을 크게 뚫었더니 이번엔 형태를 대체했다(슬리퍼가 띠 셋, 눈이 아령).
  구멍은 디테일을 더하는 도구지 형태를 바꾸는 도구가 아니다
- 폭발에 가운데 구멍 → **톱니바퀴**로 보였다. 고리 + 규칙적 이빨은 기계의 그림이다
- `node -e '...'` 인라인에 작은따옴표가 든 치환 문자열을 넣어 bash 가 끊었다 →
  `Orient: ,` 같은 깨진 코드를 뿌렸다. 따옴표가 들어가면 **스크립트를 파일로 쓴다**

**측정**: 컴파일 통과, DB 검증 27스킬, 6계열 틴트 시뮬레이션 정상,
실플레이에서 뼈다귀가 세워져 「뼈」로 읽히는 것 확인.

**남은 관찰**: 화면상 이펙트 크기가 캐릭터의 6할 정도다. `ApplyHitScale` 이
판정 반경에 맞춰 크기를 정하는데(정직한 표시), 「눈에 띄게」와 맞바꿀지는 판단이 필요하다.

---

## K · 「그린 것 ≠ 보이는 것」의 원인 (완료)

그림을 24종 다 그려 놓고도 화면에서는 말이 안 되게 나왔다. **그리는 문제가 아니라
재생 문제였다.** 원인 셋이 겹쳐 있었다.

### 1. 연출이 0.25초 만에 얼어붙었다

`Art2DBuilder` 가 FX 프리팹을 **20fps · 반복 없음**으로 구웠다. 5프레임이면 0.25초에 끝나고
마지막 프레임에서 정지한다. 3초를 사는 장판은 나머지 2.75초가 정지 화면이었다.

### 2. 그 정지 프레임이 투명했다

내가 그린 마지막 프레임은 페이드로 **알파 0** 이었다. 1회 재생 + 마지막 고정이므로
연출이 끝나면 아무것도 없었다. `fade()` 하한을 0.55 로 올리고 반복 재생으로 바꿨다.

### 3. `SkillVisual.Play()` 가 아무 일도 안 했다

이 함수는 **ParticleSystem 만** 돌리고 있었다. 그런데 이 프로젝트의 FX 프리팹에는
파티클이 **하나도 없다** — 전부 스프라이트 프레임 애니다. 매 발사마다 불렸지만 무의미했고,
애니는 풀에서 꺼내질 때 `OnEnable` 이 되감는 것에만 의존했다.

이제 `Play()` 가 `SpriteAnimator` 를 잡고 **스킬 수명에 맞춰 fps 를 정한다** —
`DespawnAfterTime` 이 있으면 그 시간에 한 바퀴, 없으면 0.45초. 6~14fps 로 묶어
정지 화면도 깜빡임도 안 되게 했다. ParticleSystem 참조는 전부 지웠다.

> `SpriteAnimator` 는 이미 있었다 — Animator/AnimationClip 없이 배열 인덱싱으로 도는
> 커스텀 컴포넌트다. 적 수백 마리에서 컨트롤러 평가 비용을 내지 않으려고 그렇게 만들었다.
> 만들 것이 아니라 **연결할 것**이었다.

## L · 걷기가 미끄러져 보이던 이유 (완료)

`WALK = [(-1,0), (0,-1), (1,0), (0,-1)]` — 두 가지가 잘못돼 있었다.

1. 몸통 상하 이동이 **1픽셀**. 픽셀 걷기에서 몸이 위아래로 뛰는 것은 장식이 아니라
   걷는다는 **주된 단서**다. 없으면 「미끄러지며 다리만 떠는」 것으로 보인다.
2. 1번과 3번 프레임이 **완전히 같았다.** 4장을 그렸는데 실제 포즈는 셋뿐이었다.

→ `[(-1.0, 1), (-0.35, -2), (1.0, 1), (0.35, -2)]`. 접지에서 내려가고 통과에서 떠오르며,
통과 두 장은 기우는 쪽을 달리해 구분한다. 다리·팔 진폭도 5→7 / 4→5.5.

**적은 상하 이동이 아예 없었고 대기 그림도 없었다.** 화면을 채우는 것이 적이라
물량이 많을수록 그 어색함이 크게 보인다. `draw_alien` 에 bob 을 넣고
`alien_<키>_idle*` 을 따로 냈다 (없어서 걷기 첫 장을 대기로 쓰느라, 멈춰도 한쪽 다리가
나간 자세로 굳어 있었다).

**측정**: 컴파일 통과, 실플레이에서 뼈다귀 · 눈빛 레이저가 형태로 읽히는 것 확인.

---

## M · 도트 품질 — 레퍼런스와의 차이를 숫자로 (완료)

받은 도트 시트와 우리 것을 픽셀 단위로 비교했다. 차이는 인상이 아니라 구조였다.

| | 고치기 전 | 레퍼런스 | 고친 뒤 |
|---|---|---|---|
| 접지 그림자 | **0픽셀** | 전원 발밑에 어두운 타원 | 개 rx13·알파160 |
| 음영 단계 | 2단 (몸통 + 뒷면 0.76) | 3~4단 | 3단 (1.16 / 1.0 / 0.82) |
| 외곽선 비중 | 개 **43%** · 적 몸통의 90% | 얇게 | **26%** |
| 이펙트 : 캐릭터 | 0.6배 | 1배 이상 | 장식 1.45배 |

- **접지 그림자가 없던 것**이 탑다운에서 「떠 보임」의 주범이었다. 가장 싸고 효과가 크다.
  호출부가 방향별로 다섯 군데라 이름(`dog_` · `alien_` · `boss_ufo`)으로 `save()` 에서 한 번에 붙인다 —
  한 곳만 빠지면 그 방향만 떠 보인다.
- **외곽선이 그림을 잡아먹고 있었다.** `fin()` 이 레이어마다 2px 선을 씌우고 그걸 쌓는 구조라
  겹치는 자리마다 선이 누적됐다. 1px 로 낮췄다.
- 이펙트는 **반경이 없는 장식만** 1.45배로 키웠다. 반경이 있는 것은 「보이는 크기 = 맞는 범위」라
  부풀리면 사거리를 오인한다 — 하한만 1.15 로 올렸다.

### 남은 비율 문제 (판단 필요)

`deco_tree` 실루엣이 **190px**, 개가 **61px**. 같은 PPU(32)라 나무가 개의 **3.1배**다.
레퍼런스 시트는 캐릭터 · 이펙트 · 소품이 한 눈금인데 우리는 나무만 크게 튄다.
정렬은 정상이다(캐릭터 10 > 데코 8) — 가리는 게 아니라 어수선한 것이다.
환경 아트를 임의로 바꾸지 않고 남겨 둔다.

---

## N · 도트 다듬기 + 한 눈금 (완료)

### 개는 이족보행이다 (네 발로 바꿨다가 되돌림)

**[사고] 「그렇게 바꿔」를 나무와 네 발 둘 다로 읽고 네 발까지 바꿨다. 나무만이었다.**
확인 질문 두 개를 한 문단에 묶어 던져 놓고 짧은 답을 둘 다에 대한 승낙으로 해석했다.
서로 다른 판단이 필요한 것은 따로 묻는다.

이 게임의 개는 원반을 물어 던지고 슬리퍼를 집어 던지고 「개쌘놈 모드」로 각성한다 —
**손으로 뭔가를 하는 캐릭터**다. 네 발이 「진짜 개」 실루엣에 가깝지만,
이 게임이 그리는 개는 그 개가 아니다.

되돌리면서 이번 작업의 개선은 전부 유지했다 — 접지 그림자 · 1px 외곽 · 명암 3단 · 걷기 바운스.
`face_paint` · `ears` · `leg` 헬퍼로 세 방향이 같은 규칙을 쓴다.

가는 꼬리(그레이하운드)는 가로 막대로 그려 놓아 **뒤로 뻗은 팔**로 보였다.
굵기가 일정한 수평 막대는 팔다리의 그림이다 — 뿌리에서 끝으로 가늘어지며 아래로 처지게 고쳤다.

### 나무를 절반으로

`deco_tree` 실루엣 190px, 개 61px → **3.1배**라 화면을 지배했다.
192×256 → 96×128 **정수 축소**(NEAREST)로 픽셀 격자를 깨지 않고 줄였다. 실루엣 95px ≒ 개의 1.6배.

### 그 과정에 찾은 이펙트 버그

화면에서 왈왈·으르렁이 **검은 덩어리**로 보였다. 틴트 색은 멀쩡했다(하늘색 · 주황).
원인은 `render()` 가 **속 찬 모양에도 안쪽을 깎아** 선을 만들고 있었던 것 —
폭 7px 조각이면 잉크가 양쪽 2px 씩 먹어 남는 밝은 부분이 3px, 잉크 비중 58~66% 였다.

만화형은 캐릭터 생성기와 같이 **바깥으로 둘러야** 밝은 면이 통째로 남는다. 고치고,
조각도 「적고 굵게」로 바꿨다(왈왈 8개 얇은 것 → 3개 굵은 것).
**잉크 비중 58~66% → 32~37%.**

### 사고 기록

`draw_dog` ~ `draw_alien` 구간을 통째로 교체하면서 그 사이에 있던 **`ALIENS` 정의를 같이 지웠다.**
git 추적 밖이라 되돌릴 수 없었고, 남아 있던 적 스프라이트에서 색 · 크기 · 눈 수 · 안테나 · 다리를
역산해 복원했다. 함수 사이에 데이터 정의가 끼어 있으면 「함수 교체」가 데이터 삭제가 된다 —
구간 교체 전에 그 안에 무엇이 들어 있는지 먼저 본다.

---

## O · 개 실루엣 다시 잡기 (완료)

이족보행으로 되돌렸지만 여전히 어색했다. 9배로 확대해 세로 구간별 실루엣 폭을 재 보니
원인이 다섯이었다 — **인상이 아니라 구조였다.**

| 부위 | 문제 |
|---|---|
| 팔 | 각진 둥근사각이 옆에 붙어 팔이 아니라 **몸의 돌기**로 보였다 |
| 다리 | 몸통(폭 30)에서 갑자기 좁아진(20) 각진 기둥. **발이 없었다** |
| 목 | 머리 35 · 몸통 30 — 잘록함이 없어 한 덩어리 |
| 허리 | y34~44 가 계속 폭 30 으로 평평. 가슴/엉덩이 구분 없음 |
| 이음새 | 몸통과 다리가 다른 레이어라 사이에 외곽선이 생겨 **붙여 놓은 것**처럼 보였다 |

### 고친 방식

- **팔을 뺐다.** 64px 안에서 팔은 3~4px 밖에 못 쓰는데, 그 크기에서는 무엇을 그려도 혹으로 읽힌다.
  없어도 아무도 팔을 찾지 않는다.
- **몸통과 가까운 쪽 다리를 같은 레이어에.** `stack()` 이 레이어마다 외곽선을 씌우므로
  나눠 두면 반드시 이음새가 생긴다. 먼 쪽 다리만 뒤 레이어에 어둡게 둔다.
- **발을 붙였다.** 앞으로 살짝 나온 타원. 없으면 다리가 잘린 기둥으로 보인다.
- **가슴/엉덩이를 타원 둘로.** 살아 있는 몸은 위에서 아래로 넓이가 오르내린다 —
  머리(넓다) · 목(좁다) · 엉덩이(넓다) · 다리(좁다) · 발(넓다). 그 굴곡이 실루엣을 몸으로 만든다.

**측정**: 실루엣 폭이 머리 30 → **목 20** → 엉덩이 31 → 다리 23 → **발 28** 로 굴곡이 생겼다
(예전에는 30 → 30 → 30 → 20 으로 평평했다). 비율 상수는 `CHEST_Y` · `HIP_Y` · `LEG_*` 로 모아 두어
세 방향이 같은 값을 쓴다.

---

## 권장 순서

```
A-1 · A-2 · B-0 · B-1 · B-2 · B-4 · C-2 · G  (완료)

다음:
**목록의 모든 블록 완료.**

이어서 볼 만한 것 (새 목록이 필요하면 그때 다시 뗀다)
- 도감/스킬 트리에 해금 표시가 들어갔으니, 도감도 잠긴 캐릭터를 실루엣으로 (지금은 3종 전부 해금)
- 사운드를 진짜 음원으로 교체 (지금은 절차 생성 자리 채움)
- 신규 이펙트 7종을 한 장씩 카메라에 담아 최종 확인
- 키보드 입력 실제 조작 확인 (하네스는 입력을 우회한다)
```

- **A** 를 먼저 하는 이유: 지금 빌드가 실제로 클리어되는지 확인되지 않은 상태다
- **C-2 를 C-1 보다 먼저**: 세로로 못 보면 모바일 조작을 판정할 수 없다
- **E-3 은 E-1·E-2 뒤**: 저장과 해금이 없으면 상점에 팔 것이 없다
