# GameDev 작업내역

## 회차 1 — 뱀서류 비(非)기획 작업 일괄 (2026-08-22)
- 확정: 웨이브 이벤트는 DB(`modules/wave`)로 — time/repeat/count/배수(hp·dmg·scale·xp)/banner. 적 감지는 「항상 추격」(60/80u). 시작 스킬은 전부 자동 발동(SlotKey None). XP 곡선 `10+(lv-1)*8+(lv-1)²`. 스폰 램프 1.0s/1 → 0.25s/8 @600s
- 산출:
  - DB/코드: `_DataExporter/GameData/modules/wave/{elite_rush,boss}.js`, `WaveEventRegistry.cs`, `WaveEventData.Generated.cs`, `Enemy.ApplyEventMultipliers/XpValue/IsElite`, `WaveDirector.TickEvents` + `OnBanner`, `SurvivorHud` 배너
  - 로비: `UI/Runtime/LobbyScreen.cs`(상점·영웅·메인·스킬 4탭, 세로 1080x1920, SafeArea), `StartScreen` 에 `IsReady/Status/SelectedSkillIds/StartRun()` 노출, 레거시 가로 캔버스는 비활성
  - 하네스: `GameData/Editor/CurveCheck.cs` (`Temp/curve.request` → 300s 무적 실측 → `Temp/curve.report`)
  - 버그 수정: `SkillRegistry.LoadAsync` 완료 태스크 캐시(재시작 후 레지스트리 공백), 걷기 클립 루프(Run Fast/Idle/Yelling/Swimming/Run/Walk `m_LoopTime 1`)
- 실측:
  - 정예 러시 강제 발동: 4마리, HP 50→200(×4), XP 3→15(×5), scale ×1.3, 배너 「정예가 몰려옵니다!」, 다음 발동 60s 재예약
  - 보스 강제 발동: 1마리, HP 1500(×30), XP 120(×40), scale ×2.4, 배너 2.5s 후 자동 숨김
  - 사망→재시작→재시작 3회: 레지스트리 wave=2/enemy=3 유지, 경고 0건, 7s 6킬
  - 곡선(무적·시작 스킬 1001~1004, 증강 자동 선택):

    | t(s) | Lv | 누적XP | 킬 | 생존 | FPS |
    |---|---|---|---|---|---|
    | 60 | 6 | 195 | 67 | 12 | 71 |
    | 180 | 13 | 1381 | 406 | 23 | 57 |
    | 300 | 19 | 3638 | 1010 | 49 | 49 |

    (튜닝 전 1차: 90s 이후 킬 정지·300 상한 고착 → 감지 거리 사고(함정 17). 2차: Lv29/5분·생존 5~35 → 과소 스폰. 3차가 위 표)
  - 로비 스크린샷 `Temp/lobby_main.png`, `Temp/lobby_skill.png`, 인게임 `Temp/lobby_ingame.png`
- 발견:
  - 이펙트 팩 일부 VFX 가 현재 카메라 거리(19u)에서 화면을 덮는다 — 아트 반입 시 스케일 규격 필요
  - FPS 5분에 49 (에디터, 적 49 + 누적 팝업/젬). 빌드 실측 미실시
  - 증강은 StatBuff/GrantSkill 2종뿐. 스킬별 파생 증강(예: 번개 쿨타임 감소)은 **데이터 구조가 없다** → 기획 요청서 참조
  - 상점·영웅 탭은 자리만. 캐릭터 컨셉·재화 기획 대기

## 회차 2 — 「개많이잡아」 기획 (2026-08-24)
- 확정: 게임명 `개많이잡아`, 3D 뱀서류. 세계관 Dog-World / God-사료 / 개쌘놈(슈퍼독) vs 외계인
  - 증강 구조 = ①스킬 획득 ②특성 증강. **증강 1개 = 특성 1개만**(복합 금지). 특성 7종 + 스킬 고유 1종
  - 스킬 20종(공용, 유저 귀속 해금) + 캐릭터 고유 스킬 3종. 증강 총 90종
  - 런 시작 = 고유 스킬 1 + 로비에서 고른 시작 공용 스킬 1 (슬롯 4칸 중 2칸)
  - 아웃게임 스킬 카드 레벨 Lv15, `기본값 × (1 + 0.08×(Lv-1))`. 적용 순서 `카드레벨 → 증강 중첩`
  - 적 방어력 없음. `스킬 피해 = 공격력 × 계수% × 치명타 배수`, `실쿨 = 기본쿨 ÷ (1 + 공속%)`
  - 세션 길이 10분
- 산출:
  - 기획서: 노션 「3D 뱀서류 게임 기획」 (코어 재미 · 스킬 20종+증강 · 캐릭터 고유 스킬 · 밸런스 검산)
  - `GameDev_스킬시스템_구현메모.md` — 스킬 23종 엔진 노드 매핑, id 규칙, 미구현 목록
  - `GameDev_난이도곡선.md` — 적 5종 스펙, 시간별 체력·스폰 곡선, 위협 vs 처리량 대조표
  - `.claude/GameDev/GameDev_Design.md` 에 「마케팅을 고려한 기획」 절 추가 (범용 규칙)
- 검산 (계산, 실측 아님):
  - 스킬 20종 단일 DPS 125~27 (공격력 100 기준). 하위권은 전부 광역이 대가 → 의도된 편차로 판단
  - 여유 배수(처리량÷위협): 0분 5.0 → 5분 3.6 → 7분 2.2 → 10분 1.01
- 발견 / 조정:
  - 버프·유틸 스킬 4종이 슬롯 값어치 미달 (슬롯 1칸 ≈ 전체 딜 25%인데 개쌘놈 모드는 평균 +10%) → 업타임 50%로 재조정
  - 뒹굴기 상시 장판 계수 40% 과함 → 30% 하향
  - 시바 고유 스킬(개무시)은 피해가 없어 시작 DPS 0 → 시작 공용 스킬 1개 동반 장착으로 해결
  - 1~2분 구간 여유 12배로 지나치게 널널 → 초반 스폰 상향 또는 정예 러시 앞당기기 필요 (실측 후 판단)
  - **엔진 최대 구멍: `ModifySkill` 없음.** 현 증강은 전역 StatBuff + `requiresSkillId` 게이트뿐이라
    「이 스킬만 피해 +30%」가 불가능. 특성 증강 90종 전체가 여기에 걸려 있다 (1순위)
  - 코기 `TargetRadial`(사방 발사), 그레이하운드 이동 궤적 장판, 시바 `Taunt`/`DamageReduce` 도 노드 신규
- 미측정:
  - 처리량 표 전체가 계산값 (CurveCheck 하네스 미실행, 현재 300s → 600s 확장 필요)
  - 광역 실제 타격 수(밀집 배율), 5분 이후 레벨 외삽, 동시 생존 상한·FPS

## 회차 3 — 장르 전환: 파밍 로그라이크 (2026-08-24)
- 확정: 뱀서류 3택1 폐기 → **2D 탑다운 12분 파밍 로그라이크**. 이유는 「3택1은 성장이 안 보인다」
  - **파츠 = 스킬 1개 + 옵션 슬롯.** 6부위(입·발·꼬리·머리·목·등) × 4종 = 공용 24종. 등급 = 옵션 슬롯 수(일반1/희귀2/전설3)
  - 파츠는 **런 한정**(로그라이크). 필드 드랍으로만 획득
  - 캐릭터 고유 파츠가 부위 하나를 **잠근다** (시바=목, 코기=꼬리, 그레이하운드=발)
  - 레벨업은 자동 스탯 상승만, 선택지 없음 (체력 +4% / 공격력 +2% per Lv)
  - 맵 1장 고정 200×200, 파밍 포인트 위치만 매 판 랜덤. 12분에 보스 「납치선」
  - 2D 전환 — 충돌이 이미 XZ 평면이라 로직 변경 거의 없음. 카메라만 오쏘그래픽
- 산출:
  - 노션 기획서 전면 재작성 (코어 재미 · 파츠 24종 + 옵션 · 필드 파밍 포인트 5종 · 캐릭터 3종)
  - `GameDev_난이도곡선.md` 재작성 — 배회형 밀도 관리, 파워 곡선, 보스 검산
  - `GameDev_스킬시스템_구현메모.md` 재작성 — 구조 전환 표, 2D 전환 비용, 부위 배치, 미구현 3순위
  - 신규 파츠 4종 설계 (꼬리 채찍 · 꼬리 프로펠러 · 귀 쫑긋 · 제3의 눈) — 부위당 4종을 맞추기 위해
- 검산 (계산, 실측 아님):
  - 10마리 처치 시간이 12분 내내 1.0~1.4초로 평평 (0분만 2.5초 — 알몸 시작 압박)
  - 보스 HP 45,000: 카드 Lv1 균형 빌드 71초 / 카드 Lv15 34초
  - 빌드별 보스 소요: 광역 몰빵 118초 / 균형 71초 / 단일 몰빵 50초
- 발견:
  - **광역/단일 트레이드오프가 이 게임의 유일한 빌드 압박이다.** 파밍은 광역이 유리한데 보스는 단일.
    이게 없으면 파츠 선택이 「좋은 거 먹기」로 퇴화한다. 보스 HP를 쉽게 내리면 안 됨
  - **레벨업 수치를 키우면 게임이 죽는다.** 공격력 +2%/Lv 이상이면 레벨이 주 성장축이 되어 파밍 동기가 사라짐
  - 0~2분이 가장 위험 (알몸 + 기본 물기). 시작 지점 근처 택배 상자 1개 보장 배치 필요
  - **엔진 1순위 3개**: `ModifySkill`(파츠 옵션 효과) / 파츠 인스턴스 개념(원형 vs 런타임 옵션) / 부위 6칸 장착·교체
  - 성능이 밸런스보다 먼저 걸린다 — 12분 목표 120마리는 회차 1 실측(49마리 FPS 49)의 2.4배. 풀링·컬링 선행 필수
  - 2D 전환 비용은 로직 쪽이 거의 0 (충돌이 이미 XZ 2D). 아트는 전량 재제작이나 Free Stickman 이 임시였으므로 순증 아님
  - 파츠 장착 표현에 **부위별 피벗 좌표 6종 규격**이 선행 필요 — 아트 착수 전 확정
- 미측정:
  - 파워 곡선·파츠 획득 페이스(20~25개) 전부 계산값. CurveCheck 720초 확장 + 배회형 스폰 교체 후 실측 필요
  - 동시 120마리 성능, 12분 Lv30 외삽
- 미정:
  - 외형 스타일 (2D 스프라이트, 레퍼런스 수집 중). 옵션 90여 종 네이밍이 강아지 컨셉에 묶여 있어 소재 변경 시 전량 재작업
  - 파츠 교체 UI, 보스 패턴, 사료·슈퍼 DNA 획득량과 해금 비용

## 회차 4 — 2D 뱀서류 전환 구현 (2026-08-26)
- 확정: 장르는 2D 뱀서류. 넓은 필드 파밍은 폐기하되 **파츠 시스템은 유지**(3택1 대신 필드 드랍 상자로 파츠 획득)
  - 월드는 XZ 평면 유지, 카메라만 오쏘그래픽 쿼터뷰(pitch 50°, orthoSize 8.85) + 스프라이트 빌보드
    → 충돌·타겟팅·발사 로직을 하나도 안 고치고 룩만 2D 가 됐다
  - PPU 32 (64px = 2유닛). 기존 휴머노이드 스케일(캐릭터 ~1.8유닛)과 그대로 맞는다
  - 레벨업은 자동 스탯만 (체력 +4% / 공격력 +2% per Lv). 3택1 패널 제거
- 정리 (73MB 삭제):
  - `_Art/100BestEffectPack`(3D VFX 58MB) · `Free Stickman`(3D 모델) · `Shaders/Outline_Lit` · 아웃라인 머티리얼
  - `CharacterAnimator`(Animator 기반) · `OutlineMaterialFactory` · `SmoothNormalBaker` · `PrefabScriptStripper` · `SkillVfxBuilder`(3D 구체 VFX)
  - 증강 시스템 전체 (`SkillAugmentRegistry` · `SkillAugmentData` · HUD 3택1 패널)
- 아트 파이프라인:
  - 이펙트 팩 → `_Art/FX` 16종 186프레임. **흰색 변형(row 5)만 반입하고 런타임 틴트** — 색은 DB(SkillVisual.color)가 정한다. 파일 수가 1/6
  - Tiny Swords 타일셋 → `_Art/Tiles`. 잔디 내부 타일을 4x4 로 엮은 256px 반복 텍스처 + 바위/덤불
  - 캐릭터 스프라이트 39장 생성 (`tools/build_chars.py`). **외곽선을 그리지 않고 실루엣을 2px 팽창**시켜 만든다 → 두께가 항상 일정.
    부위를 레이어로 나눠 각각 외곽선을 씌워야 팔/다리 분리선이 생겨 걷는 게 읽힌다 (1차 시도는 실루엣 하나만 씌워서 팔다리가 묻혔다)
  - PNG 231개의 .meta 를 직접 생성 (에디터 없이 import 설정 확정). GUID 는 경로 md5 로 고정 — 재실행해도 참조가 안 끊긴다
- DB (JS 유지):
  - `tools/gen_parts.js` 스펙 표 하나로 스킬 27 / 파츠 27 / 옵션 105 / 적 5 / 웨이브 2 를 생성
  - 부위 6칸(입·발·꼬리·머리·목·등) × 4종 = 공용 24, 캐릭터 고유 3
  - enum `EPartSlot`·`EPartTrait` 추가, config 에 `part`·`option` 모듈 + `enemy.Sprite` 컬럼 추가, `augment` 모듈 제거
  - 익스포터 검증 통과 (JSON 11 + C# 4 생성)
- 엔진 (1순위 3개 전부 구현):
  - **`SkillModifier`** — 파츠 옵션이 「이 스킬만」 보정. 적용점은 `SkillNodeData.GetFloat/GetInt` 한 곳뿐이고,
    노드 데이터(공유물)에 값을 굽지 않고 조회 시 곱해서 내보낸다. 발사체는 `SkillConfig` 가 보정을 들고 있어
    날아가는 중에 파츠를 바꿔도 그 탄은 안 변한다
  - **파츠 인스턴스** — 같은 파츠라도 개체마다 옵션이 다르므로 원형(PartData)과 인스턴스를 분리
  - **부위 6칸 장착/교체** — `PartLoadout`. 고유 파츠가 부위 하나를 잠근다
  - 부수: `SkillObject.AddPartSkill/RemoveAutoSkill`, `Damageable.AddMaxHp`, `SpriteAnimator.SetFps`
- 미검증 (중요):
  - **컴파일을 확인하지 못했다.** UnityMCP 가 ConnectionRefused 상태라 에디터 조작·콘솔·플레이 테스트 불가
  - 프리팹/씬은 `Art2DBuilder`(에디터 메뉴 Tools/개많이잡아/2D 에셋 빌드)가 만든다. 아직 실행 전이라 프리팹 0개
  - 밸런스 실측 전혀 없음

## 회차 5 — 2D 전환 검증 (2026-08-30)
- 방법: UnityMCP 가 ConnectionRefused 라 **배치모드 CLI** 로 직접 돌렸다.
  `Unity.exe -batchmode -quit -executeMethod Art2DBuilder.BuildAll` 로 컴파일+에셋 빌드,
  `Temp/runloop.request` 를 만들고 배치 실행해 RunLoopCheck(75초 플레이)로 코어 루프 실측.
  배치모드는 씬 백업 복구 다이얼로그를 아예 띄우지 않아 `_Recovery` 가 생기지 않는다.
- 컴파일: **에러 0**. 잡은 것 3건
  - `SkillModifier` 에 `using Jinhyeong_GameData` 누락 (ESkillParamKey)
  - `RunLoopCheck._panelOpen` 미정의 (3택1 제거 잔재)
  - `SurvivorHud.ShowResult` 의 `_augmentPanel` 잔재
- 에셋 빌드: 프리팹 19개(FX 16 + PartBox + XpGem + Ground), 바닥 머티리얼, Addressable 등록 완료
  - 프리팹 저장 실패 2건 해결: 삭제한 Free Stickman 이 **중첩 프리팹**으로, CharacterAnimator 가
    **missing script** 로 남아 Unity 가 저장을 거부했다 → 빌더에 `IsPrefabAssetMissing` 자식 제거 +
    `RemoveMonoBehavioursWithMissingScript` 단계를 넣어 해결
  - `Fx_Hit`/`Fx_XpGem` Addressable **주소 중복** 제거 (옛 3D 프리팹 항목). 중복이면 어느 쪽이 잡힐지 알 수 없다
- 런타임 버그 3건 (실측으로만 나왔다):
  - `DefaultLoadout.asset` 이 옛 스킬 id(1001~1012) 참조 → 비움. 파츠 체제에선 로드아웃이 스킬을 주지 않는다
  - **`SkillObject.Start` 의 `EquipAll()` 이 파츠가 준 스킬을 통째로 지웠다.** Start 가 비동기라
    파츠 착용보다 늦게 돈다 → `CompiledSkill.FromPart` 로 출처를 남기고 EquipAll 이 그것만 보존하도록 수정.
    이게 없으면 자동스킬 0개로 시작한다
  - 하네스가 `obj_enemy` 를 fire-and-forget 로 올려 `EnemySpawner.Start` 의 Prewarm 이 빈 풀로 돌았다
    → 캐시에 오를 때까지 Setup 을 미루도록 게이트 추가
- **실측 (75초, 무적)**:

    | 항목 | 값 |
    |---|---|
    | 킬 | 5 |
    | 착용 파츠 | 3개 (고유 개무시가 Neck 잠금 + 으르렁 + 사료폭탄) |
    | 자동 스킬 | 3개 |
    | 버프 | 2 (자기버프 파츠 정상 발동) |
    | 테이블 | 파츠 27 / 옵션 105 / 스킬 27 |
    | timeScale | 1 (정상 복귀) |

  → **파츠 착용 → 스킬 자동 발동 → 적 사망 → 경험치** 경로가 끝까지 돈다
- 미검증:
  - **화면을 못 봤다.** 배치모드 헤드리스라 ScreenCapture 가 안 나온다. 2D 룩·스프라이트 애니·바닥 타일링은 눈으로 확인 필요
  - 밸런스: 무적을 끄면 5마리에게 둘러싸여 몇 초 만에 죽는다(적 접촉 10 × 다수). 75초 5킬은 느리다 — 초반 화력이 부족하다
  - 파츠 드랍 → 자동 착용 경로는 하네스가 제자리라 미발동

## 회차 6 — 오케스트레이션 구축 + 첫 화면 확인 (2026-08-30)
- 오케스트레이션 (`.claude/GameDev/`):
  - **`GameDev_레퍼런스분석.md` 신규** — 착수 전 병렬 해부. 재료 규약표(텍스트/스크린샷/영상프레임/플레이노트별로
    잴 수 있는 것), 네 시각 배분(기획·클라·UI/UX·아트), 산출물 4칸(관찰/수치/우리에게/비용), 병합 게이트
  - **팬아웃은 이 한 단계뿐**임을 명시. 구현을 파트별로 병렬화하면 터지는 것은 파트 사이의 이음매이고
    그때 아무도 그 이음매의 주인이 아니다 (회차 4~5의 실제 사고가 근거)
  - `_Client` 에 「에디터를 스스로 돌리는 두 경로」 절 — 브리지(룩 판정 가능) vs 배치모드(헤드리스, 룩 판정 불가)
  - `_함정사전` 19~27 추가
  - PD 첫 턴 프로토콜에 해부 단계 삽입, 문서 로딩 규칙 갱신
- 시각 검증 경로 확보: **GUI 에디터 + 요청 파일 하네스** 로 스크린샷을 얻는다. 브리지 없이도 룩을 본다
- **배치모드와 GUI 의 실측이 다르다** — 같은 하네스가 배치 5킬 / GUI 55킬. 헤드리스는 프레임 진행이 달라
  밸런스 측정에 쓸 수 없다. 로직 회귀에만 쓴다
- 고친 것 (전부 스크린샷·리포트로 발견):
  - 옛 3D 맵(`OBJ_MAP`)이 씬에 남아 **머티리얼 없는 렌더러 7개** → 마젠타. 빌더가 프리팹 원본 경로로 찾아 제거
    (이름으로 찾으면 못 잡는다 — 씬에서 인스턴스 이름이 `Map` 으로 바뀌어 있었다)
  - 그 맵을 지우자 **지면 콜라이더까지 사라져 전원 무한 낙하**(y≈-29000). 새 바닥 프리팹을
    「고정 콜라이더 루트 + 따라다니는 자식 쿼드」 구조로 재작성
  - 정예 틴트 `(0.25,0.05,0.05)` 가 2D 스프라이트를 검은 실루엣으로 만듦 → `(1,0.55,0.5)` 로 교체
  - 하네스가 플레이 종료 후 감시자를 재등록하지 않아 두 번째 트리거부터 무반응
  - 하네스에 진단 추가: 셰이더 깨진 렌더러 스캔, 카메라·플레이어·바닥 좌표, 활성 적 수
- **실측 (75초, 무적, GUI)**: 킬 58 · 레벨 6 · 착용 파츠 3 · 자동스킬 3 · 렌더러 76개 셰이더 이상 없음 ·
  플레이어 y=0.08(지면 정상) · timeScale 1 복귀
- 화면 확인: 잔디 타일 바닥 · 시바 스프라이트 · 적 무리 · HUD · 파츠 스트립 전부 정상 렌더
- 남은 것: 밸런스(무적 끄면 초반 생존 불가) · 파츠 드랍→자동착용 경로 미검증 · 적 겹침(분리 없음)

## 회차 7 — 타일맵 + 라운드 구조 + 밸런스 1차 (2026-08-30)
- 레퍼런스 확인: 사용자가 준 영상은 **탕탕특공대**(제목으로 확인). 2D 탑다운 타일 바닥
- **맵을 진짜 타일맵으로 교체**:
  - Tiny Swords 타일셋 초록 3색 + 뒤집기 3 = 6타일. 결정적 해시로 배치(되돌아와도 같은 그림)
  - 나무·바위·덤불은 **타일이 아니라 세워서** 배치 — 원본 아트가 측면 뷰라 눕히면 뭉개진다
  - 플레이어 주변 창만 채우는 스트리밍(반경 16칸). 12분 이동거리를 다 깔 수 없다
  - [사고] `CellSwizzle.XZY` 는 **위치만 바꾸고 스프라이트를 눕히지 않는다.** 타일이 세로로 선 채
    배치돼 카드가 줄지어 선 것처럼 보였다. Grid 를 -90° 회전해야 눕는다 (함정 28)
- **라운드 구조를 데이터로**: 1분 x 12라운드, 12라운드에 보스. `modules/round` 12개 +
  `RoundRegistry`. 스폰 간격·묶음·동시 상한·적 배수를 라운드가 정한다 —
  클라의 램프 수식을 걷어내 기획이 곡선을 직접 만진다
- **밸런스 1차 — 세 가지가 겹쳐 있었다**:

    | 조치 | 라운드1 생존 |
    |---|---|
    | (시작) | 4.0초 |
    | 적 접촉 피해 하향 + 초기 스폰 0 | 5.7초 |
    | **피격 무적 0.5초** 추가 | 14.6초 |
    | **자기 피해 버그 수정** + 공격력 공식 구현 | **66.1초** (킬 39, Lv5) |

  - [사고] **팀 검사가 한 경로에만** 있어 광역 스킬이 시전자를 때렸다. 수치가 1/10 이던 동안은
    묻혀 있다가 계수를 제대로 맞추자 0.2초 즉사로 드러났다 (함정 29)
  - [사고] **피격 무적이 없으면** 붙은 적 수만큼 초당 피격이 나와 어떤 수치도 안 잡힌다 (함정 30)
  - **기획서 공식이 미구현이었다**: `피해 = 공격력 x 계수%` 인데 코드가 데이터의 Damage 를
    절대 피해로 썼고, 데이터는 기획서의 1/10 이었다. `SkillObject.AttackPower` 를 넣고
    `SkillHit` 에서 곱하도록 구현 → 사료 강화·레벨업 공격력이 이제 모든 스킬에 반영된다
- 현재 상태: 라운드1 완주(66초), 라운드2에서 사망. 킬 39 / Lv5
- 남은 것: 라운드 2~12 곡선 실측(현재 라운드1만 검증) · 보스전 미검증 · 파츠 드랍→자동착용 미검증

## 회차 8 — 라운드 곡선 실측 (2026-08-30)
- 하네스 확장: 요청에 `mortal:180` 처럼 **모드+시간**을 주고, **라운드마다 표본**을 남기고,
  **파츠 상자를 우선 주우러** 가게 했다 (상자를 안 주우면 「파츠 3개로 얼마나 버티나」를 재게 된다)
- [사고] **드랍 관리자가 한 번도 안 돌았다.** `OnPlayerSpawned` 를 구독만 하고 현재 플레이어를
  확인하지 않아, 플레이어가 먼저 생성되는 하네스 경로에서 이벤트를 놓쳤다.
  상자 0개 → 파츠가 3개에서 안 늘어남 → 「라운드2에서 죽는다」로 보였다 (함정 31).
  같은 종류 사고가 이 세션에서 두 번째 (레지스트리 로드 순서에 이어)
- **실측 (필멸, 3분 요청)**:

    | 라운드 | 초 | 킬 | Lv | 생존적 | 파츠 | 공격력 | HP% |
    |---|---|---|---|---|---|---|---|
    | 1 | 0 | 0 | 1 | 1 | 3 | 100 | 100 |
    | 2 | 60 | 40 | 5 | 3 | 5 | 108 | 54 |

  생존 **106.3초** (라운드2 중반 사망), 킬 80, 파츠 5개
- 드랍 고치기 전/후: 47.6초 → 106.3초. **파츠 획득이 곡선의 주 성장축임이 실측으로 확인됐다**
- 남은 것: 라운드 3~12 미검증(2라운드에서 죽어 더 못 잼) · 보스전 미검증 ·
  라운드2 이후 생존을 늘리려면 파츠 드랍 간격(22초)이나 라운드 곡선을 손봐야 한다

## 회차 9 — 스킬 시너지 (2026-08-30)
- 방향 확정: 조합은 **두 축**으로 간다. 하나만 있으면 반쪽이다
  - **넓게(계열)** — 같은 계열 2개/4개를 모으면 그 계열 전체 강화
  - **깊게(특화)** — 한 스킬의 증강을 4개 이상 쌓으면 그 스킬만 대폭 강화(+80%)
  - 특화가 한 스킬 기준으로 계열 4단계(+40%)보다 세다. 그래야 「1~2스킬 몰빵」이 실제 선택지가 된다
- **시너지는 선택이 아니라 조건 만족 시 자동 발동.** 3택1 을 없앤 게임에서 시너지만 고르라고
  창을 띄우면 앞뒤가 안 맞는다
- 계열 6종을 스킬 27개에 부여 (연출 색과 별개 — **플레이 감각**으로 묶는다):

    | 계열 | 수 | 성격 |
    |---|---|---|
    | 파동 | 6 | 자기중심 광역 + 둔화 |
    | 투척 | 5 | 원거리, 안전 |
    | 장판 | 4 | 지나간 자리로 딜 |
    | 근접 | 5 | 붙어야 세다 |
    | 시선 | 3 | 단일 고위력 |
    | 각성 | 4 | 자기버프 |

- DB: `ESkillFamily` enum, `part.family` 컬럼, `modules/synergy` **39종**(계열 12 + 특화 27)
- 런타임: `SynergyRegistry` + `PartLoadout.RebuildSynergies()`.
  구성이 바뀌면 **착용 중인 모든 스킬의 보정을 다시 만든다** — 계열 시너지는 이미 낀 스킬에도 걸리므로
  새로 낀 것만 갱신하면 틀린다. `SkillObject.SetPartModifier` 로 갈아 끼워야 쿨다운이 안 리셋된다
- 옵션/시너지가 같은 문(`SkillModifier.AddTrait`)을 쓰게 통일. 문이 둘이면 한쪽에만 새 특성을 넣는 실수가 난다
- **실측으로 잡은 설계 결함**: 전설 파츠(옵션 3개)를 줍는 순간 특화가 떴다 —
  「깊게 판 보상」이 아니라 「등급 운」이었다. 원인은 같은 스킬을 또 주워도 **옵션이 누적되지 않고 교체**되는 것.
  → `PartInstance.Merge` 로 **같은 스킬은 옵션을 합치게**(상한 6, 중첩 한도 준수) 바꾸고 특화 조건을 4개로 올렸다.
  이게 뱀서류의 「같은 무기를 계속 키운다」에 해당하는 축이다
- **실측 (무적, 150초)**:

    | 라운드 | 초 | 킬 | Lv | 파츠 | 공격력 |
    |---|---|---|---|---|---|
    | 1 | 0 | 0 | 1 | 3 | 100 |
    | 2 | 60 | 39 | 5 | 5 | 108 |
    | 3 | 120 | 88 | 7 | 6 | 113 |

  최종 킬 144 / Lv9 / 6부위 완성. 시너지 2개 발동 — **공명**(파동 2개, 넓게) + **정전기 털 통달**(증강 4개, 깊게).
  두 축이 같은 판에서 같이 돈다
- 남은 것: 상태이상 연계(3층) 미구현 — 피해 계산에 대상 상태 확인이 필요 ·
  캐릭터별 계열 가중치(4층) 미구현 · 필멸 곡선 재측정 필요(시너지가 붙었으니 다시 재야 한다)

## 회차 10 — 시너지 도달 가능화 + 밸런스 (2026-08-30)
- **부위 제약을 걷어냈다.** 교차표를 세어 보니 부위(6칸)와 계열(6종)이 충돌해
  계열 시너지 대부분이 **도달 불가능**이었다:

    | 계열 | 종수 | 서로 다른 부위 | 가능한 단계 |
    |---|---|---|---|
    | 시선 | 3 | 1 (전부 머리) | **불가능** |
    | 장판·각성·투척 | 4~5 | 2 | 2단계까지 |
    | 근접 | 5 | 3 | 2단계까지 |
    | 파동 | 6 | 4 | 4단계 가능 |

  부위당 하나만 착용 가능하니 「그 계열 최대 착용 수 = 서로 다른 부위 수」다.
  조합을 만들라면서 조합을 막는 제약이라 **슬롯 6개만 남기고 부위 제한을 없앴다** (함정 32)
- 캐릭터 선호 계열 도입 — **고유 스킬의 계열을 그대로 쓴다**(별도 데이터 불필요).
  드랍 가중치 x2.5. 캐릭터 선택이 곧 빌드 방향이 된다
- 같은 스킬을 또 얻으면 증강을 **합친다**(상한 6). 교체만 하면 깊이 축이 없다
- 파츠 드랍 간격 22 → 12초. [실측] 22초면 생존이 43~106초로 2.4배 흔들렸다 —
  「언제 줍느냐」가 판을 정해버려 곡선을 판단할 수 없다
- [사고] 하네스가 새로 고침 직후 플레이에 진입해 도메인 리로드로 상태가 날아갔다.
  한 폴링 띄우고 들어가도록 수정 (함정 33). 이 실패로 세 번을 헛돌았다
- **실측**:

    | 조건 | 생존 | 킬 | 스킬 | 시너지 |
    |---|---|---|---|---|
    | 부위 제약 · 드랍22s | 43~106초 | 28~80 | 3~5 | 0개 |
    | 제약 해제 · 드랍12s (필멸) | 62.9초 | 40 | **6/6** | (측정 실패) |
    | 제약 해제 · 드랍12s (무적 150초) | — | **149** | **6/6** | **3개** |

  무적 150초 시너지: **지구력**(각성 2, 넓이) + **공명**(파동 2, 넓이) + **으르렁 통달**(깊이).
  두 축이 한 판에서 같이 돈다
- 남은 것: 상태이상 연계(3층) 미구현 · 필멸 곡선이 아직 라운드 2에서 끝난다(12라운드 목표 대비 부족) ·
  사망 후 씬 리로드로 하네스 참조가 끊겨 필멸 리포트 일부가 비었다


## 11회차 — 시너지 3층 완성 · 생존 축 신설 · 12라운드 곡선 · 보스/클리어

### 시너지 3층이 한 판에서 같이 돈다
- `SynergyRegistry.Collect` 에 **연계 조건**(그 상태이상을 *거는* 스킬을 들었나) 판정 추가.
  거는 쪽과 때리는 쪽이 갈려야 조합이 생긴다
- `PartLoadout` 이 장착 스킬의 `AppliesDebuff` 를 모아 넘기고, 연계는 특성이 아니라
  **대상 조건부 배수**(`VsDebuff`/`VsDebuffDamageMul`)로 실린다
- 실측(무적 150초): **시너지 6개** — 계열 2 · 연계 2 · 특화 2. 세 축이 전부 발동

### 생존 축을 새로 심었다 (이게 곡선의 절반이었다)
성장이 전부 공격이라 12분을 버틸 방법이 없었다. 특성 3종(`MaxHp`/`Lifesteal`/`MoveSpeed`)을 추가하고,
스킬별 보정이 아니라 **몸에 한 번만** 적용한다(`PartLoadout.ApplyBodyTraits`).

- [사고] 계열마다 생존 1종만 줬더니 흡혈 없는 계열이 최대 체력만 얻어 **곡선이 오히려 짧아졌다**
  (116초 → 64초). 회복 없는 생존 스탯은 공격을 희석한 만큼도 못 돌려준다 → 흡혈을 전 계열 공통으로
- [사고] 처치당 고정 회복은 후반에 처치 수와 함께 폭발했다(분당 480 회복 → 라운드 12 까지 무피해).
  **최대 체력 비례 초당 상한**(`LifestealCapPerSecond`)을 걸어 회복을 비율로 묶었다

### 하네스가 회피를 안 하고 있었다 — 실측 전체의 전제가 틀려 있었다
「가장 가까운 아이템으로 직진」만 했다. 젬은 적 시체 위에 떨어지므로 매번 적 무리 한가운데로
걸어 들어가는 조작이었고, 주울 게 없으면 제자리에 섰다.
목표 방향 + 근접 적 반발 벡터 합성(`Steer`)을 넣자 **같은 데이터에서 라운드 5 사망 → 12라운드 완주**.

### 필멸 곡선 (12라운드 = 720초 목표)
| 라운드 | 1~6 | 7~11 | 12 |
|---|---|---|---|
| HP | 100% | 93~97% | **69%** |

동시 생존 적이 131까지 쌓이며 후반 압박이 물량으로 작동한다.

### 보스 · 클리어
- `WaveEvent.IsBoss` / `EnemyTypeId` 열 신설. 보스 처치 → `GameFlow.ClearGame()` → 결과 화면 **CLEAR!**
- [사고] `boss: 1` 을 bool 열에 넣어 `AsBool()` 이 조용히 false 를 돌려줬다. 보스는 **스폰돼 있었고**
  표식만 안 붙었다 — 「미등장」 옆에 살아 있는 적의 최대 체력을 같이 찍어 한 줄로 갈랐다
- [사고] 보스가 가중치 추첨으로 나와 매판 다른 몸이었다(체력 4배차). 우락이로 고정
- [실측] 잡졸 142 마리 속에서 100초에 보스 체력 1% → 타겟팅이 「가장 가까운 적」이라 딜이 전부 잡졸로 간다.
  11라운드 상한을 100 으로 낮추고 12라운드 스폰을 사실상 멈춘 뒤 보스 체력을 8,640 으로 맞췄다

### 조작 · 비주얼 · 최적화
- **자동 조준**(`AutoAim`): 정면이 「걷는 방향」뿐이라 도망치며 싸울 때 스킬이 등 뒤로 나갔다.
  루트 회전만 돌리므로 그림(빌보드)과 판정 방향을 따로 굴린다
- **4방향 스프라이트**: 정면/뒤/옆 3벌 + 좌우 반전. 기존엔 대칭 정면 한 벌이라 flipX 가 무의미했다
- **월드 HP 바 빌보드**: 루트가 조준으로 돌면 바가 같이 눕는다
- **FX 풀 사전 생성**: 적은 200개를 미리 만드는데 피격/데미지텍스트/젬 풀은 빈 채로 시작했다

## 12회차 — 이펙트가 스킬 이름을 따라간다 · 시작 화면

### 스킬 정체성 이펙트 17종을 직접 그렸다
받아온 팩은 범용 폭발·베기뿐이라 **「물기」와 「으르렁」과 「하품」이 같은 음파 그림**으로 나갔다.
이름이 붙은 스킬은 그 이름이 화면에 보여야 한다.

`_DataExporter/tools/build_skillfx.py` — 캐릭터 생성기와 같은 규칙(색만 칠하고 실루엣 팽창 →
균일 외곽선), 흰색으로만 그려 런타임에 스킬 속성색으로 틴트된다.

| 이펙트 | 그림 |
|---|---|
| 물기 | 위아래 이빨이 벌어졌다 **맞물린다** + 충격 조각 |
| 으르렁 | 드러낸 이빨 + 퍼지는 파동 (이빨이 있어야 「짖기」와 갈린다) |
| 왈왈 | 앞으로 터지는 소리 부채꼴 + `!` |
| 하품 | 커지는 졸음 고리 + Z |
| 발톱 세례 | 발톱 자국 3줄이 순서대로 그어진다 |
| 뼈다귀 · 꼬리 후리기 · 냄새 물결 · 귀 쫑긋 · 무리 울음 | 각각 전용 |
| 사료 · 원반 · 테니스공 · 슬리퍼 · 식빵 · 발바닥 · 흙먼지 | 각각 전용 |

- 스킬 27개가 **9종 공용 → 19종 정체성**으로 갈렸다 (`sound` 하나를 5개가 돌려쓰던 것을 해체)
- [사고] 링을 4겹 겹치니 외곽선(양쪽 2px)이 서로 붙어 **검은 덩어리**가 됐다.
  링 간격은 최소 8px 필요 — 세기는 겹 수가 아니라 퍼지는 폭으로 낸다
- [사고] `t=0` 부터 샘플링하니 「퍼지는」 계열의 첫 프레임이 통째로 비었다 → `t=(i+1)/n`

### 시작 화면
제목이 `SURVIVOR` 라는 영문 자리표시자였고 나머지가 전부 글자였다. 게임 이름도 주인공도 없었다.
**이름(개많이잡아) → 한 줄 후킹 → 주인공 초상 → 시작 버튼** 순으로 세웠다.
초상화는 `Assets/Resources/Lobby/` 로 따로 낸다 — UI 는 Sprite 참조가 필요한데
스프라이트 한 장 때문에 어드레서블 그룹을 늘리는 것보다 짧다.

### 4방향 캐릭터 · 자동 조준 · 풀링 (사용자 요청)
- 정면/뒤/옆 3벌 + 좌우 반전. 기존엔 **좌우 대칭 정면 한 벌**이라 flipX 가 무의미했고,
  위아래로 걸어도 계속 정면을 봐서 방향이 안 읽혔다
- `AutoAim` — 정면이 「걷는 방향」뿐이라 도망치며 싸울 때 스킬이 등 뒤로 나갔다.
  루트 회전만 돌려 그림(빌보드)과 판정 방향을 분리
- 월드 HP 바에 빌보드 (루트가 조준으로 돌면 바가 같이 눕는다)
- FX 풀 사전 생성 — 적은 200개를 미리 만드는데 피격·데미지텍스트·젬 풀은 빈 채로 시작했다
- [사고] `Enemy.ApplyType(null)` 이 체력·피해를 리셋하지 않고 빠져나가, 뒤이은 라운드 배수가
  **이전 개체 값 위에 또 곱해졌다** — 풀 재사용마다 적 체력이 불어난다
